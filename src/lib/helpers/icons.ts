export const simpleIcon = (name: string) =>
	`https://cdn.simpleicons.org/${name.toLowerCase().replace(/\s+/g, "")}`;

export const favicon = (domain: string) =>
	`https://icons.duckduckgo.com/ip3/${domain}.ico`;
